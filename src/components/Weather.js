import React from "react";
import { Paper } from '@material-ui/core';

const Weather = () => {
    return (
        <div>
            <Paper elevation={3}/>
        </div>
    );
}

export default Weather